@model web.ViewModels.ContratoViewModel
<script C:\Users\User\Desktop\AGUS\Trabajo Grupal\E5\Aplicación\webMolino\web\wwwroot\js\site.js></script>
@{
    ViewData["Title"] = "Contratos";
}

<h1>Contratos</h1>

<form asp-action="Index" class="form-inline mt-3 mb-4">

    <div class="form-group row mx-auto">
        <div class="col-4">
            <div asp-validation-summary="ModelOnly" class="text-danger">
            </div>
            <input asp-for="Filtro" class="form-control" />
        </div>
        <div class="col-2">
            <input type="submit" value="Buscar" class="btn btn-success" />
        </div>
        <div class="col-4">
        </div>
        <div class="col-2">
            <a asp-action="Create" class="btn btn-primary">Crear</a>
        </div>
    </div>
</form>

<div id="results-container" class=" table table-responsive">
    <table id="table" class="table table-bordered">
        <thead>
            <tr>
                <th>
                    Número
                </th>
                <th>
                    Fecha emisión
                </th>
                <th>
                    Fecha límite
                </th>
                <th class="desaparecer">
                    Precio por tonelada
                </th>
                <th class="desaparecer">
                    Método de pago
                </th>
                <th>
                    Acciones
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Contratos)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Numero)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FechaEmision)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FechaLimite)
                    </td>
                    <td class="desaparecer">
                        <div class="d-inline-flex">
                            <p>$</p>@Html.DisplayFor(modelItem => item.PrecioTonelada)
                        </div>
                    </td>
                    <td class="desaparecer">
                        @Html.DisplayFor(modelItem => item.MetodoDePago)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i></a>
                        <a asp-action="Delete" asp-route-id="@item.Id"><i class="bi bi-trash" style="color:red"></i></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {
    @{
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">

    }
}